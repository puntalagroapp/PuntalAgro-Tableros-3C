# Copiá este archivo como .env y completá los valores reales
# cp .env.example .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=clave_secreta_postgres
POSTGRES_DB=puntalagro_tableros_3C
DATABASE_URL=postgresql://postgres:clave_secreta_postgres@base_datos:5432/puntalagro_tableros_3C

# Vacío = sin restricción de CORS (alcanza para desarrollo local, ya que
# frontend y API se sirven del mismo origen en http://localhost:8080).
ALLOWED_ORIGINS=

# Tiene que coincidir EXACTO con _PA_AK en frontend/login.html (login.html
# lo manda como Authorization: Bearer <API_KEY> en cada intento de login) —
# si no coincide, el login falla con "No autorizado" aunque el usuario y la
# contraseña sean correctos.
API_KEY=PA_e5f208d00ad24d859032f68d253f06ac8ca3408eb6ebf4310eb5d3a94b5dbed0
